'use strict';
const METADATA={tool_id:'tool_workflow_orchestrator',name:'Tool Workflow Orchestrator',purpose:'Coordinate the full Tool Foundry workflow for creating, repairing, testing, approving, and using backend tools.',status:'Testing',risk_level:'low',version:'0.1.0',approval_state:'pending_execution_test',builtin:false,input_schema_description:'user_goal; requested_capability; current_tool_id; current_tool_status; workflow_stage; risk_level; owner_standing_rules; available_tools; registry_result; readiness_result; autonomy_result; source_inspection_result; builder_result; validator_result; operator_result; quality_result; failure_result; dead_end_result; recent_action_summary',output_schema_description:'workflow_status; next_required_tool; next_required_action; can_continue_automatically; owner_approval_needed; owner_approval_reason; current_blocker; completion_path; tools_to_call_next; gates_that_must_pass; should_create_mission; should_generate_files; should_validate_payload; should_install; should_redeploy; should_test; should_quality_check; should_approve; should_stop; plain_english_summary'};
function text(v){if(v===null||v===undefined)return'';if(typeof v==='string')return v;try{return JSON.stringify(v)}catch(e){return String(v)}}
function has(v,terms){const s=text(v).toLowerCase();return terms.some(t=>s.includes(String(t).toLowerCase()))}
function flag(v,keys){if(v===true||v===false)return v;if(!v)return false;if(typeof v==='object')return keys.some(k=>v[k]===true||String(v[k]).toLowerCase()==='true'||String(v[k]).toLowerCase()==='passed'||String(v[k]).toLowerCase()==='approved');const s=text(v).toLowerCase();return keys.some(k=>s.includes(String(k).toLowerCase()+': true')||s.includes(String(k).toLowerCase()+'=true'))||has(s,['passed','approved','can_install true','can_install: true'])}
function avail(input,id){const raw=Array.isArray(input.available_tools)?input.available_tools:text(input.available_tools).split(/[;,\n]/);return raw.map(String).map(x=>x.trim()).includes(id)}
function rem(next){const all=['autonomy_governor','tool_readiness_checker','backend_source_inspector','executable_tool_builder','tool_installation_validator','foundry_operator','live_execution','tool_quality_tester','approval','tool_registry_auditor'];const i=Math.max(0,all.indexOf(next));return all.slice(i)}
async function execute(input={}){const combined=[input.user_goal,input.requested_capability,input.current_tool_id,input.current_tool_status,input.workflow_stage,input.risk_level,input.owner_standing_rules,input.registry_result,input.readiness_result,input.autonomy_result,input.source_inspection_result,input.builder_result,input.validator_result,input.operator_result,input.quality_result,input.failure_result,input.dead_end_result,input.recent_action_summary].map(text).join(' | ');const stage=text(input.workflow_stage||input.current_tool_status).toLowerCase();const risk=text(input.risk_level||'low').toLowerCase();const gates=['autonomy_governor confirms safe automation or owner approval is present','tool_readiness_checker checks existing approved capability','backend_source_inspector returns full untruncated source before source changes','executable_tool_builder returns explicit handler and router payload files','tool_installation_validator returns can_install=true before foundry_operator','foundry_operator completes install or redeploy without blockers','live execution passes before approval','tool_quality_tester passes before approval','tool_registry_auditor checks registry health after approval or cleanup'];const auto=flag(input.autonomy_result,['can_proceed_automatically','can_install','can_redeploy','can_test']);const owner=risk==='high'||has(combined,['unclear risk','must_ask_owner','owner approval required']);const readiness=Boolean(input.readiness_result)||has(stage,['readiness']);const source=Boolean(input.source_inspection_result)||has(stage,['source']);const built=Boolean(input.builder_result)||has(stage,['builder']);const valid=flag(input.validator_result,['can_install']);const vfail=has(input.validator_result,['can_install:false','can_install false','validation_status":"failed','validation_status: failed','failed']);const operated=flag(input.operator_result,['ok','completed','deployed','redeployed']);const live=has(combined,['live execution passed','execution passed','tool execution passed']);const quality=flag(input.quality_result,['should_mark_approved','quality_passed','passed']);const failure=(Boolean(input.failure_result)||has(combined,['failure','failed','error']))&&!quality;const loop=Boolean(input.dead_end_result)||has(input.recent_action_summary,['loop','repeat','repeated','stalled','dead end','dead-end']);const approved=has(input.current_tool_status,['approved'])||has(input.quality_result,['should_mark_approved:true','should_mark_approved true']);let workflow_status='planning',next_required_tool='autonomy_governor',next_required_action='Run autonomy_governor first with risk, approval, and safety facts.',current_blocker=null,should_generate_files=false,should_validate_payload=false,should_install=false,should_redeploy=false,should_test=false,should_quality_check=false,should_approve=false,should_stop=false,can=true;if(loop){workflow_status='blocked';next_required_tool='workflow_dead_end_resolver';next_required_action='Use workflow_dead_end_resolver before repeating the same Tool Foundry action.';current_blocker='Workflow appears to be looping or stalled.';should_stop=true;can=false}else if(failure&&!valid&&!live&&!quality){workflow_status='blocked';next_required_tool='tool_failure_diagnoser';next_required_action='Use tool_failure_diagnoser to classify the failed stage before retrying build, install, or approval.';current_blocker='A failure is present and has not been repaired.';should_stop=true;can=false}else if(!input.autonomy_result&&(stage===''||stage.includes('planning')||owner||risk==='low')){workflow_status='planning';next_required_tool='autonomy_governor';next_required_action='Run autonomy_governor first with risk, approval, and safety facts.'}else if(!readiness){workflow_status='readiness_required';next_required_tool='tool_readiness_checker';next_required_action='Check whether an approved existing tool already satisfies the requested capability.'}else if(!source){workflow_status='source_inspection_required';next_required_tool='backend_source_inspector';next_required_action='Inspect backend source in strict full-file mode before generating source changes.'}else if(!built){workflow_status='ready_to_generate_files';next_required_tool='executable_tool_builder';next_required_action='Generate explicit handler and router payload files using the full source inspection result.';should_generate_files=true}else if(vfail){workflow_status='blocked';next_required_tool='executable_tool_builder';next_required_action='Repair or regenerate the files payload, then rerun tool_installation_validator. Do not call foundry_operator.';current_blocker='tool_installation_validator blocked the payload.';should_validate_payload=true;should_stop=true;can=false}else if(!valid){workflow_status='validation_required';next_required_tool='tool_installation_validator';next_required_action='Validate the proposed file payload before foundry_operator.';should_validate_payload=true}else if(!operated){workflow_status='ready_to_install';next_required_tool='foundry_operator';next_required_action='Use foundry_operator only because validation passed and approval or autonomy is available.';should_install=true;should_redeploy=true}else if(!live){workflow_status='live_test_required';next_required_tool='live_execution';next_required_action='Run live execution tests for the new tool and required foundation tools before approval.';should_test=true}else if(!quality){workflow_status='quality_check_required';next_required_tool='tool_quality_tester';next_required_action='Run tool_quality_tester after live execution passes.';should_quality_check=true}else if(!approved){workflow_status='ready_for_approval';next_required_tool='registerTool';next_required_action='Mark the tool Approved only after live execution and quality testing have passed.';should_approve=true}else{workflow_status='approved_or_cleanup';next_required_tool='tool_registry_auditor';next_required_action='Audit registry health after approval or cleanup.'}const path=rem(next_required_tool);const tools=path.filter(x=>x==='live_execution'||x==='approval'||x===next_required_tool||avail(input,x));return{workflow_status,next_required_tool,next_required_action,can_continue_automatically:Boolean(can&&!owner&&!should_stop),owner_approval_needed:Boolean(owner&&!auto),owner_approval_reason:owner&&!auto?'Risk or approval state is unclear, so only the specific owner-level decision should be requested.':'',current_blocker,completion_path:(tools.length?tools:path).join(' -> '),tools_to_call_next:tools.length?tools:path,gates_that_must_pass:gates,should_create_mission:false,should_generate_files,should_validate_payload,should_install,should_redeploy,should_test,should_quality_check,should_approve,should_stop,plain_english_summary:current_blocker?'The workflow should stop at the current blocker and use the recommended Tool Foundry repair step before continuing.':'The workflow can continue through the next Tool Foundry gate without manual file handling or repeated owner back-and-forth.'};}
function install(router){if(!router)return;if(Array.isArray(router.BUILTIN_TOOL_METADATA)){const i=router.BUILTIN_TOOL_METADATA.findIndex(t=>t.tool_id===METADATA.tool_id);if(i>=0)router.BUILTIN_TOOL_METADATA[i]=METADATA;else router.BUILTIN_TOOL_METADATA.push(METADATA)}if(router.EXECUTABLE_HANDLERS)router.EXECUTABLE_HANDLERS[METADATA.tool_id]=execute;if(typeof router.registerTool==='function')router.registerTool(METADATA);return{installed:true,tool_id:METADATA.tool_id}}
module.exports={METADATA,metadata:METADATA,execute,handle:execute,install};