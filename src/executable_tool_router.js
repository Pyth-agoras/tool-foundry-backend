'use strict';
const STARTED_AT=new Date().toISOString();
const mutableMissions=[],mutableEvaluations=[],mutableExecutions=[];
const BUILTIN_TOOL_METADATA=[{tool_id:'idea_analyzer',name:'Idea Analyzer',purpose:'Analyze raw user ideas into a core goal, risk level, needed tool type, and next action.',status:'Approved',risk_level:'low',version:'0.2.0',approval_state:'approved',builtin:true},{tool_id:'tool_mission_generator',name:'Tool Mission Generator',purpose:'Convert a raw idea into a complete Tool Mission.',status:'Approved',risk_level:'low',version:'0.2.0',approval_state:'approved',builtin:true},{tool_id:'foundry_self_healer',name:'Foundry Self-Healer',purpose:'Diagnose Tool Foundry setup problems.',status:'Approved',risk_level:'low',version:'0.1.0',approval_state:'approved',builtin:true},{tool_id:'foundry_operator',name:'Foundry Operator',purpose:'Apply approved backend file updates, trigger Render redeploys, and verify backend readiness.',status:'Approved',risk_level:'medium',version:'0.1.0',approval_state:'approved',builtin:true},{tool_id:'tool_call_contract_normalizer',name:'Tool Call Contract Normalizer',purpose:'Normalize Tool Foundry tool-call payloads before execution so backend tools receive the correct input shape.',status:'Testing',risk_level:'low',version:'0.1.0',approval_state:'pending_execution_test',builtin:false,input_schema_description:'target_tool_id; intended_action; raw_payload; known_contract; preserve_fields; recent_failure_summary',output_schema_description:'normalized_call; target_tool_id; payload_shape_status; fields_preserved; fields_moved_into_input; missing_required_fields; warnings; should_execute; plain_english_summary'}];
const mutableTools=new Map(BUILTIN_TOOL_METADATA.map(t=>[t.tool_id,{...t}]));
function getTools(){return Array.from(mutableTools.values())}
function norm(v){return String(v||'').toLowerCase().replace(/[^a-z0-9_\s-]/g,' ').replace(/\s+/g,' ').trim()}
function toks(v){const stop=new Set(['the','and','for','that','with','into','from','this','tool','tools','backend','create','build','make','new','need','needed','given','whether','what','user','users','return','returns','using','check','checks']);return norm(v).split(' ').filter(w=>w.length>2&&!stop.has(w))}
function idea(input={}){const raw=input.raw_idea||input.idea||input.text||'';const risk=['low','medium','high'].includes(norm(input.risk_level))?norm(input.risk_level):'low';return{ok:true,core_goal:raw?String(raw).trim():'No idea provided.',intelligence_pattern:'planning',risk_level:risk,approval_required:risk==='high',approval_reasons:[],needed_tool_type:String(raw).toLowerCase().includes('pdf')?'document_analysis':'general_backend_tool',next_action:risk==='high'?'Get owner approval before implementation or execution.':'Proceed with planning or use an existing approved tool if one matches.'}}
function mission(input={}){const raw=input.raw_idea||input.idea||input.context||'new backend tool';const m={id:`mission_${Date.now()}`,status:'Draft',tool_name:toks(raw).slice(0,5).join('_')||'new_tool',user_facing_purpose:`Help the owner with: ${raw}`,capability_needed:raw};mutableMissions.push(m);return{ok:true,mission:m}}
function selfHeal(){return{status:'ready_for_operator_use',core_tools_ready:true,core_tools_present:['idea_analyzer','tool_mission_generator','foundry_self_healer','foundry_operator'],missing_core_tools:[],configured_values:{API_KEY:Boolean(process.env.API_KEY),GITHUB_TOKEN:Boolean(process.env.GITHUB_TOKEN),GITHUB_OWNER:Boolean(process.env.GITHUB_OWNER),GITHUB_REPO:Boolean(process.env.GITHUB_REPO),GITHUB_BRANCH:Boolean(process.env.GITHUB_BRANCH),RENDER_DEPLOY_HOOK_URL:Boolean(process.env.RENDER_DEPLOY_HOOK_URL),PUBLIC_BASE_URL:Boolean(process.env.PUBLIC_BASE_URL)},current_tools:getTools(),counts:{tools:getTools().length,missions:mutableMissions.length,evaluations:mutableEvaluations.length,executions:mutableExecutions.length}}}
async function putFile(path,content){const owner=process.env.GITHUB_OWNER,repo=process.env.GITHUB_REPO,branch=process.env.GITHUB_BRANCH||'main',token=process.env.GITHUB_TOKEN;if(!owner||!repo||!token)throw new Error('GitHub write settings are not configured.');const api=`https://api.github.com/repos/${owner}/${repo}/contents/${path.split('/').map(encodeURIComponent).join('/')}`;const headers={Authorization:`Bearer ${token}`,Accept:'application/vnd.github+json','User-Agent':'tool-foundry-backend'};let sha;const current=await fetch(`${api}?ref=${encodeURIComponent(branch)}`,{headers});if(current.ok)sha=(await current.json()).sha;const put=await fetch(api,{method:'PUT',headers:{...headers,'Content-Type':'application/json'},body:JSON.stringify({message:`Update ${path}`,content:Buffer.from(content,'utf8').toString('base64'),branch,sha})});if(!put.ok)throw new Error(`GitHub update failed for ${path}: ${put.status} ${await put.text()}`);const body=await put.json();return{path,commit:body.commit&&body.commit.sha}}
async function op(input={}){const result={mode:input.mode||'diagnose',started_at:new Date().toISOString(),diagnosis_before:selfHeal(),actions_taken:[],blockers:[],results:{}};if(Array.isArray(input.files)&&input.files.length){if(!(input.approved===true&&input.approval_confirmed===true)){result.blockers.push('Owner approval and approval confirmation are required before backend file updates.');result.next_action='Get owner approval before applying file changes.';return result}result.results.github_updates=[];for(const f of input.files)if(f&&f.path&&typeof f.content==='string'){result.results.github_updates.push(await putFile(f.path,f.content));result.actions_taken.push(`updated:${f.path}`)}if(process.env.RENDER_DEPLOY_HOOK_URL){const d=await fetch(process.env.RENDER_DEPLOY_HOOK_URL,{method:'POST'});result.results.render_deploy={skipped:false,ok:d.ok,status:d.status};result.actions_taken.push('render_deploy_triggered')}else result.results.render_deploy={skipped:true,reason:'RENDER_DEPLOY_HOOK_URL not configured'}}result.results.diagnosis=selfHeal();result.next_action=result.blockers.length?'Resolve blockers before continuing.':'Operator completed the requested action.';return result}
function registerTool(record={}){if(!record.tool_id){const e=new Error('tool_id is required.');e.statusCode=400;throw e}const previous=mutableTools.get(record.tool_id)||{};const next={...previous,...record,builtin:Boolean(record.builtin??previous.builtin)};mutableTools.set(record.tool_id,next);return{tool_id:record.tool_id,status:next.status||'Draft',message:'Tool registered.'}}
function createMission(record={}){const m={id:`mission_${Date.now()}`,status:'Draft',...record};mutableMissions.push(m);return{mission_id:m.id,status:m.status,mission:m}}
function getMissionStatus(id){const m=mutableMissions.find(x=>x.id===id);if(!m){const e=new Error('Mission not found.');e.statusCode=404;throw e}return m}
function evaluateTool(record={}){const tool=mutableTools.get(record.tool_id);const ev={tool_id:record.tool_id,status:tool?'ready_for_execution_verification':'not_found',score:tool?0.85:0,recommendation:tool?'Verify one live execution before approval.':'Register or install the tool before evaluation.'};mutableEvaluations.push(ev);return ev}
const EXECUTABLE_HANDLERS={idea_analyzer:idea,tool_mission_generator:mission,foundry_self_healer:selfHeal,foundry_operator:op};
async function executeTool(tool_id,input={}){const h=EXECUTABLE_HANDLERS[tool_id];if(!h){const e=new Error('No executable handler is installed for this tool.');e.statusCode=404;throw e}const result=await h(input);mutableExecutions.push({tool_id,at:new Date().toISOString()});return result}
const routerApi={STARTED_AT,BUILTIN_TOOL_METADATA,EXECUTABLE_HANDLERS,getTools,executeTool,registerTool,createMission,getMissionStatus,evaluateTool,selfHeal};
function installExternal(modulePath){try{const mod=require(modulePath);if(mod&&typeof mod.install==='function')return mod.install(routerApi);if(mod&&mod.metadata&&typeof mod.handler==='function'){EXECUTABLE_HANDLERS[mod.metadata.tool_id]=mod.handler;registerTool(mod.metadata);return{installed:true,tool_id:mod.metadata.tool_id}}}catch(error){return{installed:false,modulePath,error:error.message}}return{installed:false,modulePath,error:'No install(router) or metadata+handler export found.'}}
routerApi.external_install_results=[installExternal('./backend_source_inspector'),installExternal('./executable_tool_builder'),installExternal('./tool_failure_diagnoser'),installExternal('./tool_quality_tester'),installExternal('./tool_installation_validator'),installExternal('./tool_registry_auditor'),installExternal('./workflow_dead_end_resolver'),installExternal('./autonomy_governor'),installExternal('./tool_workflow_orchestrator'),installExternal('./note_cleanup_tool'),installExternal('./tool_call_contract_normalizer')];
module.exports=routerApi;
