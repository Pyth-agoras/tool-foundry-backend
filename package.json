{
  "name": "trusted-repository-writer",
  "version": "1.0.0",
  "private": true,
  "description": "Fail-closed branch writer with hash, validation, approval, test, deployment, verification, rollback, and audit gates.",
  "type": "commonjs",
  "bin": {
    "trusted-repository-writer": "bin/trusted-repository-writer.js"
  },
  "scripts": {
    "test": "node test/run.js"
  },
  "engines": {
    "node": ">=18"
  }
}